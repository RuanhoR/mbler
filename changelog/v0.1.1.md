# v0.1.1 (2026-01-25)

### Features

- **MCX compiler progress**: Continued development of the MCX DSL compiler (~40KB of compiler code)
- **Improved TypeScript support**: Enhanced type system across the project
- **HTML parser**: Added HTML comment handling and nested HTML parsing
- **AST analysis**: Completed initial AST analysis phase for MCX files

### Chores

- Removed `mime-types` dependency (dropped `web_edit` feature)
- Added `posthtml-parser` dependency
- Moved project documentation to dedicated website
- Package metadata improvements (CLI tag, etc.)
