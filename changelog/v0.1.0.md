# v0.1.0 (2026-01-17)

### Breaking Changes

- **TypeScript migration**: Entire codebase migrated from JavaScript to TypeScript
- **Code processor rewritten**: `code-processor` fully ported to TypeScript

### Features

- **JSON minification**: Minify JSON files during production builds (reintroduced in new architecture)
- **TypeScript definitions**: Added proper type declarations for all modules

### Fixes

- Fixed build command failure in specific scenarios
- Fixed language set error during initialization
- Fixed init command showing `[object Object]` when project already initialized
