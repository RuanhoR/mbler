# v0.0.7 (2025-12-20)

### Features

- **JSON minification**: Minify JSON files in all directories except `scripts/` during build
- **Error command prediction**: Smarter command correction — input `mbler buid` outputs "Did you mean `mbler build`?"

### Refactor

- Replaced native `switch` statement with object lookup for better maintainability in command routing

### Fixes

- Fixed zip archive root path incorrect after `processDist`
